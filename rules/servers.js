const UNKNOWN_VERSION = '';
const testRules = {
  Nginx: {
    id: 'nginx',
    icon: 'nginx',
    url: 'http://nginx.org/en',
    test(headers) {
      headers.map((header) => {
        })
    }
    },
  },
};

